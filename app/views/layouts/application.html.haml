!!!
%html
  %head
    %title Open Source It With Me
    = stylesheet_link_tag 'reset', 'main'
    = javascript_include_tag 'jquery', 'rails', 'application'
    = csrf_meta_tag
  %body
    #wrapper
      #header
        %h1
          = link_to 'Open Source It With Me', root_path
          #user_nav
            - if current_user
              Welcome 
              = current_user.name
              = link_to 'Sign Out', signout_path
            - else
              = link_to "Sign in with Github", "/auth/github"
          
        %h2
          A simple way for developers to pair up for short sessions
          
      #issues
        - [:notice, :error, :message].each do |key| 
          - unless flash[key].blank? 
            %p{ :class => "flash flash_#{key}" }= flash[key]

        
      #content
        = yield
    
      #footer
        Contact: brad.cantin at gmail dot com
        = link_to 'app roadmap', roadmap_path